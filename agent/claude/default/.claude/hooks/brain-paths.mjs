// brain-paths.mjs — the per-OS roots the brain hooks read. **windows branch.**
//
// The only hook file that differs between AtlasAta-DotFiles' platform branches; every other hook
// comes from `main`. The arch-caelestia branch carries its own copy.
export const VAULT = "C:\\obsidian\\root";
export const DEV_ROOT = "C:\\dev";
